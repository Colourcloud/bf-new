/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['blog.builtflat.co.nz', 'secure.gravatar.com'], // Replace with your WordPress domain
      },
}

module.exports = nextConfig
